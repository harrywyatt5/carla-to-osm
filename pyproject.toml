[project]
name = "carla-to-osm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "lxml>=6.1.1",
    "pyproj>=3.7.2"
]

[project.optional-dependencies]
carla = [
    "carla>=0.9.16"
]
